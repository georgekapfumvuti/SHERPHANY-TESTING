# How to make it work?

Use `npm install` to install dependencies
Then use `npx cypress open` for opening cypress
After configuring Cypress UI for e2e select the test files to launch automated testing
